{% block stylesheets %}
    <link rel="stylesheet" href="/css/menu.css">
{% endblock stylesheets %}

{% block content %}

    <div class="mainMenu">
        {# Boucle IF qui vérifie si une image est bien en base de données #}
        {% set plat = menu.plats|first %}
        <img src="/uploads/plats/{{plat.photo}}" alt="{{ plat.nomPlat }}" width="30%">
        <div class="divMenu">
            <h3>{{ menu.titre }}</h3>
            <p>Thème : {{ menu.theme.description }}</p>
            <p>Régime : {{ menu.regime.description }}</p>
            {% for plat in menu.plats %}
            <p>{{ plat.nomPlat }}</p>
            {% endfor %}
            <p>{{ menu.description }} Lorem ipsum dolor sit amet,<br>
                consectetur adipiscing elit, sed do eiusmod tempor <br>
                incididunt ut labore et dolore.</p>
            <p>Allergène []</p>
            <p>● {{ menu.prixPersonne }}€ par personne</p>
            <p>● À partir de {{ menu.personneMini }} boîte à repas</p>
            <p>{{ menu.qttRestante }} quantités disponibles</p>
            <a href="#">Liste des menus</a>
            <a href="#">Commander</a>
        </div>
        <a href="{{ path('app_employe_gestion_menu') }}">Retour</a>
    </div>

{% endblock content %}