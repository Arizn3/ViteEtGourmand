{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="/css/menus.css">
{% endblock stylesheets %}

{% block title %}Nos menus{% endblock title %}

{% block content %}

    <h2>Ce que nous vous proposons</h2>
    <br>
    {# Filtre #}
    <input type="number" id="prixMax" placeholder="Prix max. par personne">
    <select id="theme">
        <option value="">Tous les thèmes</option>
        {% for theme in themes %}
            <option value="{{ theme.id }}">{{ theme.description }}</option>
        {% endfor %}
    </select>
    <select id="regime">
        <option value="">Tous les régimes</option>
        {% for regime in regimes %}
            <option value="{{ regime.id }}">{{ regime.description }}</option>
        {% endfor %}
    </select>
    <input type="number" id="nbPersonne" placeholder="Boîte à repas">
    <br>

    <div class="mainMenus" id="menuList">
        {# Boucle FOR IN qui itère dans l'objet qui est en fait l'Entité Menu #}
        {% for menu in menus %}
            <div class="divMenus">
                <p>{{ menu.titre }}</p>
                {% set plat = menu.plats|first %}
                <img src="/uploads/plats/{{plat.photo}}" alt="{{ plat.nomPlat }}" width="100%">
                <p>{{ menu.description }}</p>
                <p>► {{ menu.prixPersonne }}€ par personne</p>
                <p>► À partir de {{ menu.personneMini }} boîte à repas</p>
                <a href="{{ path('app_menu_detail', {id: menu.id}) }}">Détail</a>
            </div>
        {% else %}
            <p>Aucun menu disponible</p>
        {% endfor %}
        </div>

{% endblock content %}

{% block javascripts %}
    {{ parent() }}
    <script src="/javascript/menus.js"></script>
{% endblock javascripts %}