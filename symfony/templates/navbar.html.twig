{# BOUTON ACCUEIL #}
<div class="navAccueil">
    <a href="{{ path('app_home') }}">Accueil</a>
</div>

{# ESPACE MENUS #}
<div class="navMenus">
    <a href="{{ path('app_menu') }}">Nos Menus</a>
</div>

{# NOTRE HISTOIRE #}
<div class="navHistoire">
    <a href="#">Notre histoire</a>
</div>

{# CONNEXION / INSCRIPTION #}
{% if not app.user %}
    <div class="navLogin">
        <a href="{{ path('app_login') }}">Connexion /</a>
        <a href="{{ path('app_register') }}">Inscription</a>
    </div>
{% endif %}

{# ESPACE UTILISATEUR #}
{% if app.user and is_granted('ROLE_USER') %}
    <div class="navUtilisateur">
        <button class="btnNavUtilisateur">Mon Espace</button>
        <div class="lienUtilisateur">
            <a href="{{ path('app_utilisateur_information_personnelle') }}">Compte</a>
            <a href="{{ path('app_utilisateur_commandes') }}">Commandes</a>
        </div>
    </div>
{% endif %}

{# ESPACE EMPLOYE #}
{% if app.user and is_granted('ROLE_EMPLOYE') %}
    <div class="navEmploye">
        <button class="btnNavEmploye">Espace Employé</button>
        <div class="lienEmploye">
            <a href="{{ path('app_employe_commande') }}">Commandes</a>
            <a href="{{ path('app_employe_gestion_menu') }}">Menus</a>
            <a href="{{ path('app_employe_avis') }}">Avis</a>
            <a href="{{ path('app_employe_horaire') }}">Horaire</a>
        </div>
    </div>
{% endif %}

{# ESPACE ADMINISTRATEUR #}
{% if app.user and is_granted('ROLE_ADMIN') %}
    <div class="navAdmin">
        <button class="btnNavAdmin">Espace Administrateur</button>
        <div class="lienAdmin">
            <a href="{{ path('app_administrateur_stat') }}">Statistiques</a>
            <a href="{{ path('app_admin_nouveau_employe') }}">Compte</a>
        </div>
    </div>
{% endif %}

{# CONTACT #}
<div class="navContact">
    <a href="#">Contact</a>
</div>